<% if flash[:notice] %>
<%= flash[:notice] %>
<% end %>

<%= render partial: 'shared/sample', locals: { book: @book_new, user: @user } %>


<div class="col-md-7">
<h3>Books</h3>
<table class="table">
  <tr><th>title</th><th>opinion</th></tr>
  <% @books.each do |book| %>
  <tr><td><%= link_to book.title, book_path(book) %></td><td><%= book.body %></td></tr>
  <% end %>
</table>

</div>